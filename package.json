{
  "name": "socotra-factory",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ poomch/socotra-protocol",
    "create-local": "graph create --node http://localhost:8020/ poomch/socotra-factory",
    "remove-local": "graph remove --node http://localhost:8020/ poomch/socotra-factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 poomch/socotra-factory"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}